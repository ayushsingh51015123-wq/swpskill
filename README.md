# SkillSwap

Track 2 creator marketplace project.

- `index.html` — frontend
- `backend/` — Express booking API
- `DECISIONS.md` — Decision Points

Run backend with `cd backend && npm install && npm start`. GitHub Pages can host the frontend, but the Node backend must be deployed separately for a public demo.